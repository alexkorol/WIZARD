# WIZARD repo — agent pointers

Multi-subproject repo (gh-pages). Per-subproject agent docs:

- **RPG inventory asset generation (Verdigris)** — the active recurring task.
  Read `tools/rpg_inventory/AGENTS.md`, then run
  `python3 tools/rpg_inventory/core/status.py` to see the queue.
  Goal + budget: `tools/rpg_inventory/core/GOAL.md`.
  Mechanical loop: `tools/rpg_inventory/core/RUNBOOK.md`.
- `tools/wizard_orbs/` has its own `CLAUDE.md`.

Do not touch subprojects unrelated to the task you were given.

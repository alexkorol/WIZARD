Repaint your last EIGHT-FRAME sheet at the much coarser PIXEL SIZE demonstrated by this attached single sprite. The attachment is ONLY a pixel-size example, never a pose reference. Keep every one of the eight existing poses, positions, figure sizes, linen costume and colors. Preserve all eight different motion phases. All pixels must be square 5x5 blocks like the example; each figure uses only about 73 logical pixels head-to-foot. Simplify cloth and skin into large color clusters; absolutely no fine pores, weave, strand detail, smooth edges or subpixel shading. Return the full 1536x1024 eight-frame sheet with identical layout. Use imagegen background="transparent". Keep true alpha zero around every character and between limbs. Transparent PNG, no checkerboard, background or halo.
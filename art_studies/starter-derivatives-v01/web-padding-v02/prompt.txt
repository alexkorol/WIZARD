Create one square transparent PNG with background="transparent". Paint ONLY the FOUR poses in this new Image 1, arranged TWO COLUMNS by TWO ROWS. Use the coarse square pixels and gritty pixel shading of Image 2.

This is a 256×256 logical-pixel sheet enlarged 4× with nearest neighbor. Each quarter is 128×128 logical pixels: a centered 96×96 character frame plus 16 transparent pixels on every side. The woman is only about 78 logical pixels tall. Image 2 is one quarter at exactly the SAME magnification. Keep her that small in each quarter, with exactly the large empty margins shown in Image 1. Do not enlarge her to fill a quarter. The entire output should have only 256 coarse pixel columns and 256 coarse pixel rows, even if delivered as a larger PNG. Paint solid square pixel clusters without fine lines, antialiasing or smaller detail.

Preserve the four Blender poses, limb overlaps, camera, positions and facing right, in reading order. Same young woman, brown braid, ivory sleeveless tunic, russet trim and cord belt, open sandals, unarmed. No added poses or weapons. Output the sheet alone. Preserve real alpha in all empty regions; no checkerboard, floor, background, labels or drawn grid. Use the transparent background setting.

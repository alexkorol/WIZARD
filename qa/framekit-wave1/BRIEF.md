# QA brief — FRAMEKIT wave-1 landed tree (post PR #95/#96)

- **Lane:** `wizard-desktop-tvu7or7-cursor-1` (harness `cursor`, model **Cursor Grok 4.6**)
- **Tree under test:** `origin/codex/arcane-lattice-1-0` `eac48a1` (merges of PR #95 FK-101 and PR #96 FK-102).
- **Serve:** `python -m http.server 8162 --bind 127.0.0.1` from the lane worktree (capsule 8162–8163).
- **Verdict:** **FAIL** the board's "every demo, zero console errors" and strict token-literal gates. Most component demos now pass. Findings only; no product-code edits.

An earlier pass at `2fcbae7` (pre-merge) failed almost every demo on missing `tokens/tokens.css`. That hole closed when #95/#96 merged. This brief is the post-merge re-run.

## Checklist

| # | Gate | Result |
| - | ---- | ------ |
| 1 | Owned-path packet diff | N/A (QA, no claim) |
| 2 | No build; vanilla HTML/CSS/ESM over loopback HTTP | **PASS** for files that exist. Kit **`index.html` still 404**. |
| 3 | Component CSS `--fk-*` only (`0` / `100%` / `var(--fk-x, #000)`) | **FAIL** (`modal.css`). |
| 4 | `node --check` + every demo, **zero console errors** | `node --check` **PASS**. **FAIL** showcase, assets demo, window demo, missing index. Other demos **PASS** (0 console lines). |
| 5 | Evidence | this brief, `qa/framekit-wave1/transcript.txt`, `qa/framekit-wave1/evidence/*.png` |

## Commands + output

```
python -m http.server 8162 --bind 127.0.0.1
```

```
$ node --check tools/gui_framekit/components/controls/tabs/tabs.js
exit=0
```

```
$ python tools/gui_framekit/tools/generate_assets.py --check
OK: 10 assets reproduce byte-for-byte
exit=0
```

`HEAD http://127.0.0.1:8162/…` (post-merge):

- **200:** showcase, assets demo, all FK-102 frame demos, all FK-103–106 demos, `tokens/tokens.css`, frame/control/hud/inventory/overlay CSS, orb sprites, `panel.png`/`slot.png`.
- **404:** `/tools/gui_framekit/index.html`
- **404:** `/tokens/tokens.css` (wrong href from `assets/demo.html`)
- **404:** `/tools/gui_framekit/components/assets/textures/window-frame.png` (broken relative `url()` from `window.css` / `dialog.css`; file also does not exist under `assets/textures/` — FK-107 shipped `panel.png` and `slot.png` only)

Edge (`channel=msedge`) console:

- Component demos for controls, hud, inventory, overlays (except as below), panel, dialog: **0 console lines**.
- `demo/showcase.html`: 2× `Failed to load resource: 404` (the missing `window-frame.png`, referenced twice).
- `assets/demo.html`: 404 `/tokens/tokens.css`.
- `components/frames/window/demo.html`: 1× 404 (`window-frame.png`); also `import "./window.js"` (file exists).
- `index.html`: 404 page.

Screenshots: `qa/framekit-wave1/evidence/`. Post-merge button/showcase shots show the dark token theme actually applying.

## Defects (proposed wave-1.5)

**A — `assets/demo.html` token href is one `../` too deep.**
`tools/gui_framekit/assets/demo.html` → `../../../tokens/tokens.css` → `/tokens/tokens.css`. Should be `../tokens/tokens.css`. Same page uses `border-image-width: 12px` / slice `12`.

**B — frame nine-slice URL + missing texture.**
`window.css` and `dialog.css`: `url("../../assets/textures/window-frame.png")` resolves under `components/assets/…`, not `tools/gui_framekit/assets/textures/`. No `window-frame.png` was generated by FK-107. Showcase therefore cannot meet zero-console.

**C — `modal.css` token misses.**
`background: rgba(0, 0, 0, 0);` and `max-width: min(90vw, …)` in `components/overlays/modal/modal.css`.

**D — kit entry `index.html` absent** on the program branch (FK-108 owned `index.html`; landed showcase is `demo/showcase.html` only).

Demo chrome `max-width: 420px` (and similar) in several `demo.html` `<style>` blocks — call if the token gate covers demos.

## Out of scope

Wave 2 still queued / not claimable. No READY packets. This lane did not edit `tools/gui_framekit/` product files.
